import React from 'react';
import styles from './Header.module.scss';

const Header = () => {
    return (
        <div className={`${styles.appHeader}`}>Info z drogi</div>
    )
};

export default Header;